{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker yt-download-server Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node"
        }
    ]
}
